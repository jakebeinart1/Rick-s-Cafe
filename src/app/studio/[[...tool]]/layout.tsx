export const metadata = {
  title: "Rick's Cafe Studio",
  description: "Content management for Rick's Cafe",
};

export default function StudioLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body style={{ margin: 0 }}>{children}</body>
    </html>
  );
}
